function solve(input) {

    input = input.split('');
    let result = [];

    for (let i = 1; i < input.length; i++) {
        let currentWord = '';

        if (input[i].toLowerCase() === false) {
            currentWord+= input[i];
        }
    }
}

solve('SplitMeIfYouCanHaHaYouCantOrYouCan');