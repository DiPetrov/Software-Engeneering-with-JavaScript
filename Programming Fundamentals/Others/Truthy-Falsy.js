// Тук показваме как се работи с булиан флагове с цел прекъсване на цикли

let total = false;

console.log(total);

total = true;

if (total) { // Същото като if (total === true) {}
 console.log(total);
 
}

