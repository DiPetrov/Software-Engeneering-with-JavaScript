let number = 10;                          // Number
let person = {name: 'George', age: '25'}; // Object
let array = [1, 2, 3];                    // Array
let isTrue = true;                        // Boolean
let name = 'George';                      // String
let empty = null;                         // null
let unknown = undefined;                  // undefined


/* Strings in JavaScript are immutable! */
/* Changes a single letter in a word. The value hat to be saved in a new variable! */
let word = 'Str_ng';
let symbol = 'I';
let result = word.replace('_', symbol); // word = 'StrIng' 

/* Booleans Examples in JavaScript */
Boolean(undefined);        // false
Boolean(0);                // false
Boolean(-0);               // false
Boolean('');               // false
Boolean(false);            // false
Boolean(null);             // false
Boolean(10 / 'p');         // false


